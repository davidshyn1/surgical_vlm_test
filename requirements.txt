# surgical_vlm_test — portable Python deps (no CUDA-specific wheels)
#
# 1) Install PyTorch for YOUR CUDA/CPU stack first (not pinned here):
#      https://pytorch.org/get-started/locally/
#    Example (CUDA 12.4):
#      pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#    Example (CPU only):
#      pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#
# 2) Then install the rest:
#      pip install -r requirements.txt
#
# Optional (not required for most eval tasks):
#   opencv-python-headless   # MP4 fallback when ffmpeg unavailable
#   openai google-genai anthropic   # cloud API backends

# --- core (HF VLM eval) ---
transformers>=4.52.1
accelerate>=0.30.0
huggingface-hub>=0.23.0
safetensors>=0.4.0
pillow>=10.0.0
numpy>=1.24.0,<3.0.0

# --- PEFT LoRA adapters (e.g. surgsigma_qwen3vl_full) ---
peft>=0.11.0

# --- InternVL3.5 ---
timm>=1.0.0
einops>=0.7.0

# --- visual cross-attention (patch features + viz) ---
matplotlib>=3.8.0
scipy>=1.11.0

# torchvision is used by patch_feature_extractors (install with torch above)
